// Generated by jextract

package com.sernamar.jgit2.bindings;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef void (*git_credential_ssh_interactive_cb)(const char *, int, const char *, int, int, const LIBSSH2_USERAUTH_KBDINT_PROMPT *, LIBSSH2_USERAUTH_KBDINT_RESPONSE *, void **)
 * }
 */
public class git_credential_ssh_interactive_cb {

    git_credential_ssh_interactive_cb() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        void apply(MemorySegment name, int name_len, MemorySegment instruction, int instruction_len, int num_prompts, MemorySegment prompts, MemorySegment responses, MemorySegment abstract_);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.ofVoid(
        git2.C_POINTER,
        git2.C_INT,
        git2.C_POINTER,
        git2.C_INT,
        git2.C_INT,
        git2.C_POINTER,
        git2.C_POINTER,
        git2.C_POINTER
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = git2.upcallHandle(git_credential_ssh_interactive_cb.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(git_credential_ssh_interactive_cb.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static void invoke(MemorySegment funcPtr,MemorySegment name, int name_len, MemorySegment instruction, int instruction_len, int num_prompts, MemorySegment prompts, MemorySegment responses, MemorySegment abstract_) {
        try {
             DOWN$MH.invokeExact(funcPtr, name, name_len, instruction, instruction_len, num_prompts, prompts, responses, abstract_);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

